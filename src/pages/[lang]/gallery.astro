---
import Layout from "../../layouts/Layout.astro";
import GalleryImages from "../../components/GalleryImages";

export function getStaticPaths() {
	return [
		{ params: { lang: "en" } },
		{ params: { lang: "lv" } },
		{ params: { lang: "ru" } },
	];
}
const { lang } = Astro.params;

---

<Layout>
	<GalleryImages client:only />
</Layout>

