---
import Layout from "../../../layouts/Layout.astro";
import Collection from "../../../components/Collection";
import ProductsItimes from "../../../components/ProductItems";

export function getStaticPaths() {
	return [
		{ params: { lang: "en" } },
		{ params: { lang: "lv" } },
		{ params: { lang: "ru" } },
	];
}

---

<Layout>
	<Collection brand='bbold' client:only />
</Layout>

