---
import Layout from "../../../layouts/Layout.astro";
import Collection from "../../../components/Collection";
import ProductsItimes from "../../../components/ProductItem";

export function getStaticPaths() {
	return [
		{ params: { lang: "en" } },
		{ params: { lang: "lv" } },
		{ params: { lang: "ru" } },
	];
}
const { lang } = Astro.params;

---

<Layout>
	<Collection lang={lang} brand='bbold' client:only />
</Layout>

